# AGENTS

- Keep changes small and readable.
- Commit each completed change with `jj`, then move `master` to `@` and create a fresh working copy.
- Prefer preserving current KDE tray behavior unless a task explicitly changes it.
